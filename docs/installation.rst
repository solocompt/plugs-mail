============
Installation
============

At the command line::

    $ easy_install plugs-mail

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-mail
    $ pip install plugs-mail
